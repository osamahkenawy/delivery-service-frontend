# Use relative /api path so it works from any device via Vite proxy
# For direct backend access from LAN, uncomment and set your LAN IP:
# VITE_API_URL=http://10.156.254.50:4001/api
VITE_API_URL=/api
